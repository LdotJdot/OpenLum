不要 sessions_spawn。

在 `D:\Desktop\fly报奖` 下、**仅针对文本资料**（grep 请设 path 为该目录且 **glob 为 `*.{md,txt}`**，避免扫入 `*.py` 等脚本）用 **grep** 分别检索（可分两轮或一轮多工具调用）：
1) 含「科学技术奖」或「报奖」的句子（择一合理 pattern）；
2) 含「环境」且与「研究院」或「研究所」同句或邻近的表述（合理 regex）。
汇总：每个关键词组各有多少文件命中、各举一条短摘录；用简短纯文本（不要用 markdown 标题、列表或 ``` 代码围栏）。禁止用 exec 搜索。
